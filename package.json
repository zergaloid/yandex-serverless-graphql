{
  "name": "manydb-graphql",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc schema.ts",
    "fn:deploy": "serverless deploy",
    "deploy": "npm run build && npm run fn:deploy"
  },
  "dependencies": {
    "graphql": "^15.6.0",
    "graphql-helix": "^1.8.0",
    "typescript": "^4.4.3"
  },
  "devDependencies": {
    "serverless-yandex-cloud": "0.1.3"
  }
}
